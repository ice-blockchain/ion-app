# Run with `dart run ffigen --config ffigen.yaml`.
name: IonContentLabelerBindings
description: |
  Bindings for `src/ion_content_labeler.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/ion_content_labeler_bindings_generated.dart'
headers:
  entry-points:
    - 'src/ion_content_labeler.h'
  include-directives:
    - 'src/ion_content_labeler.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
