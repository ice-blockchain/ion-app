import 'package:flutter/material.dart';

class AuthenticatorCodeConfirmPage extends StatelessWidget {
  const AuthenticatorCodeConfirmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
